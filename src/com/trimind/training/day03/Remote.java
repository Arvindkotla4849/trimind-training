package com.trimind.training.day03;

/*public class Remote
{
      private static int currentChannel;
      private  int volumeLevel;
      private boolean currentState;

    /*  public static void raiseVolume()
      {
          volumeLevel++;
          System.out.println("the volume has raised" +volumeLevel);
      }
      public static void lowerVolume()
      {
          volumeLevel--;
          System.out.println("the volume has lowered" +volumeLevel);
      }
*/     /* public static  void increaseChannel()
      {
          currentChannel++;
          System.out.println("the channel number has increased" +currentChannel);
      }
      public  void decreaseChannel()
      {
          currentChannel--;
          System.out.println("the channel number has d6ecreased"+currentChannel);
      }
      public void switchOff()
      {
         currentState=true;
          System.out.println("either tv is on or off, please on" currentState);
      }

   /* public int getCurrentChannel() {
        return currentChannel;
    }

    public void setCurrentChannel(int currentChannel) {
        this.currentChannel = currentChannel;
    }

    public int getVolumeLevel() {
        return volumeLevel;
    }

    public void setVolumeLevel(int volumeLevel) {
        this.volumeLevel = volumeLevel;
    }

    public boolean isCurrentState() {
        return currentState;
    }

    public void setCurrentState(boolean currentState) {
        this.currentState = currentState;
    }*/

